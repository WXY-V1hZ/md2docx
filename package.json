{
  "name": "md2docx",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "check": "bun tsc --noEmit && bun run oxlint && bun run oxfmt"
  },
  "devDependencies": {
    "oxfmt": "^0.58.0",
    "oxlint": "^1.73.0"
  },
  "dependencies": {
    "remark-parse": "^11.0.0",
    "remark-stringify": "^11.0.0",
    "unified": "^11.0.5"
  }
}
